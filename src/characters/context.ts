import type { Locale } from '../core/language/locale.ts';
export const contextLimit: Record<Locale, string> = {
  ru: 'У меня нет дополнительных оснований, чтобы обосновать эту мысль дальше.',
  en: 'I have no further grounds to justify that thought.',
};
