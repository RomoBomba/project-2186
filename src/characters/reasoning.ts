import type { Locale } from '../core/language/locale.ts';
// An epistemic boundary, not an additional scientific/philosophical claim.
export const partialBoundary: Record<Locale, string> = {
  ru: 'Этого недостаточно для более сильного вывода в этом случае.',
  en: 'This is not sufficient for a stronger conclusion in this case.',
};
